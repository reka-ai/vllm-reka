# ABOUTME: GPU integration tests requiring vllm and GPU hardware.
# ABOUTME: Placeholder for forward pass, embedding pipeline, and full vllm integration tests.

import pytest

pytestmark = pytest.mark.gpu
